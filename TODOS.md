# Examer.WebUI 重构待办

This File is Generated by GitHub Copilot. 

## 目标
- 降低组件文件复杂度，避免 View 层承担数据访问与数据整形的全部职责。
- 统一请求入口，禁止在 View/Store 中直接分散发起 axios 请求。
- 将 payload 校验、payload 构建、接口调用解耦，提升可维护性与可测试性。
- 规范命名与目录结构，逐步淘汰 src/ts 的“杂物间”角色。

## 当前问题（基于代码扫描）
- 多个页面直接调用 axios，导致请求逻辑散落在 View/Store 中。
- 部分 interface 仍定义在 Vue 单文件组件或工具文件内部。
- 个别页面脚本职责过多（数据拉取、分页拼接、业务流程、动画、UI 状态混在一起）。
- src/ts 命名语义过宽，难以体现分层。

## P0

### 1. 建立统一 HTTP 层
- [x] 新建 src/api/http/client.ts，迁移 src/ts/request.ts 中 axios 实例。
- [x] 将拦截器拆分为 src/api/http/interceptors/auth.ts 与 src/api/http/interceptors/error.ts。
- [x] 新建 src/api/http/request.ts，提供统一 request 方法（包含泛型响应类型、错误归一化）。
- [x] 约束：View/Store 不允许直接 import axios，只能调用 api 模块。

验收标准
- src 下不再出现 from "axios"（仅 http 层例外）。
- 登录过期处理、消息提示、路由跳转逻辑仅存在于拦截器层。

### 2. API 模块化（按领域拆分）
- [x] 新建 src/api/modules/auth.api.ts。
- [x] 新建 src/api/modules/user.api.ts。
- [x] 新建 src/api/modules/exam.api.ts。
- [x] 新建 src/api/modules/group.api.ts。
- [x] 新建 src/api/modules/commit.api.ts。
- [x] 新建 src/api/modules/file.api.ts。
- [x] 将现有页面中的请求逐步迁移为模块函数调用。

验收标准
- 页面中只保留业务流程调用，如 await examApi.getGroups(...)。
- 所有 URL 与 HTTP 方法集中在 api/modules 中。

### 3. payload 校验与构建分离
- [ ] 新建 src/validation 目录，按领域放置 payload 校验函数。
- [ ] 新建 src/mappers（或 src/builders）目录，负责 UI 表单到 DTO 的转换。
- [ ] 每个写操作遵循：validate -> build payload -> api call。
- [ ] 先迁移问题编辑相关逻辑，再迁移用户、分组、提交相关逻辑。

验收标准
- 组件中不再出现大量 payload 字段手工清洗/trim/Number 转换代码。
- 校验失败逻辑可单测，不依赖组件上下文。

## P1

### 4. interface 全量外置
- [ ] 将 Vue 文件内部 interface 迁移到 src/types 下。
- [ ] 细分类型目录：src/types/domain、src/types/api、src/types/ui。
- [ ] 每个模块仅维护同领域类型，避免单文件过长。
- [ ] 为历史命名不规范类型做统一（例如 option 改为 Option）。

优先迁移文件
- [ ] src/views/Exam/GroupView.vue（ExamGroupResponse）。
- [ ] src/views/Exam/CandidateView.vue（ExamUserResponse）。
- [ ] src/views/Problem/EditView.vue（ProblemFormValue 等）。
- [ ] src/views/Footer/LicenseView.vue（Project）。
- [ ] src/ts/fileUpload.ts（FileDto）。

验收标准
- View 组件中无 interface/type 定义（极少量局部类型可例外，但需有理由）。
- 类型索引导出清晰，避免循环引用。

### 5. 单文件瘦身（先处理重灾区）
- [ ] 给视图层设定复杂度阈值：script 超过 250 行必须拆分。
- [ ] 将大页面拆到 composables：
  - useExamGroupData
  - useGroupAssignment
  - useUserPaginationFetch
- [ ] 动画逻辑抽离到独立 hook，避免业务逻辑与动画耦合。

验收标准
- 重点页面脚本行数明显下降。
- 每个 composable 仅负责一类能力（拉取、转换、交互流程）。

## P2

### 6. 命名与目录重构（替代 src/ts）
- [ ] 目录重命名策略：
  - src/ts/request.ts -> src/api/http/client.ts
  - src/ts/fileUpload.ts -> src/services/file/uploadFile.ts
  - src/ts/previewFile.ts -> src/services/file/previewFile.ts
  - src/ts/deleteConfirm.ts -> src/services/dialog/deleteConfirm.ts
  - src/ts/encrypt.ts -> src/utils/crypto/encrypt.ts
  - src/ts/entry.ts -> src/bootstrap/entry.ts
- [ ] 执行后统一修复 import 路径与别名。

命名约定
- API 文件名：xxx.api.ts
- 业务服务：动词开头，如 uploadFile.ts、assignExamGroups.ts
- 类型名：PascalCase，DTO 后缀用于请求/响应契约
- Hook/composable：useXxx.ts

验收标准
- src/ts 目录最终清空并删除。
- 文件名可从名字直接判断职责。

### 7. 请求可靠性与可观测性
- [ ] 统一错误对象结构（业务错误码、HTTP 状态、用户提示语）。
- [ ] 写操作增加幂等保护或防重复提交策略（按钮 loading + request key）。
- [ ] 为关键 API 增加超时与重试策略（仅幂等 GET）。

验收标准
- 页面错误提示一致，不再出现同类错误多种文案。
- 重复点击不会触发重复写入。

## 建议执行顺序
- [ ] 第 1 步：先做 HTTP 统一层和 API 模块骨架（不改业务行为）。
- [ ] 第 2 步：迁移一个完整业务链路（如 Problem/Edit）验证模式。
- [ ] 第 3 步：批量迁移剩余页面请求。
- [ ] 第 4 步：统一类型与目录重命名。
- [ ] 第 5 步：补充单测与回归测试。

## Definition of Done（完成定义）
- [ ] View/Store 中不再直接依赖 axios。
- [ ] 组件中 payload 校验与 payload 构建已拆分。
- [ ] 主要页面接口类型全部迁移到 src/types。
- [ ] src/ts 被替代为语义化目录并清理。
- [ ] 项目可正常 build，关键流程回归通过。
